# MVP — пошаговый чек-лист реализации (для сверки)

**Создан:** 2026-07-25
**Основа:** `docs/plan_dorabotok_full.md` + `docs/audit_deep_national_platform_2026-07-24.md`.
**Границы MVP:** этапы 4–8 (`docs/26`), до трека развития. Ничего из существующего не урезаем.
**Как пользоваться:** отмечаем `[x]` по мере готовности; каждый пункт готов только когда выполнен его критерий приёмки.
**Правила работы:** ветка на блок, тесты к каждому изменению, без `reset --hard`/`--force` в main, коммит/пуш — только по просьбе владельца.

---

## ФАЗА A — безопасность и психобезопасность (до реальных детей)

### A1. IDOR — единый контроль доступа
- [x] Создан `app/lib/authz/requireOwnedResource.ts` (`getPrincipalUserId`: principal из серверной сессии + resolve mergedInto)
- [x] `diagnostic/results` выбирает строго по владельцу на всех ветках (кэш/report/промежуточный); клиентский `session_id` не даёт доступа
- [x] Ключ кэша отчёта привязан к владельцу (`report:<userId>`) — и в `results`, и в `next-question:943`
- [x] `next-question`: убран `user_id` из query (сервер восстанавливает по `session_id`); клиент (`diagnosticStore`) больше не шлёт `user_id`
- [ ] `register-session` / `submit-answer` / `delete-last-answer`: владение гостевой сессией → **закрывается механизмом A2 (capability)**, т.к. эти маршруты обслуживают гостей по random-UUID
- [x] Негативный тест «Алиса не видит отчёт Боба» + 401-без-сессии + happy-path + кэш-по-владельцу — зелёные (`results/route.test.ts`, 4/4)
- [x] Полный набор тестов зелёный (12 файлов, 2957); `tsc --noEmit` = 0

### A2. Безопасное слияние гостевой сессии
- [x] `app/lib/authz/guestCapability.ts` — HMAC-подпись `<sessionId>.<hmac>`, constant-time проверка (тест 4/4)
- [x] Одноразовая HttpOnly-capability ставится при создании гостевой сессии (`coach/chat`)
- [x] Merge только при валидной capability на этот `sessionId`; кука гасится после
- [x] Merge обёрнут в `prisma.$transaction` (delete+update+rename+mergedInto атомарно)
- [x] Отклонённый merge не берёт чужую сессию — берёт свою по `userId`
- [x] tsc=0; полный набор зелёный (13 файлов, 2961)
- [ ] Playwright E2E cross-user/race — вынести в отдельный прогон (нужен сервер); юнит-инвариант (подделка невозможна) закрыт

### A3. Кризисный протокол (Safety)
- [x] `app/lib/coach/safety.ts` — `detectDistress` (правила: суицид/самоповреждение/насилие/отчаяние) + `CRISIS_REPLY`
- [x] Встроен в `coach/chat` ДО обработки/записи; системные сообщения пропускаются
- [x] При срабатывании коуч прерывает сценарий, даёт контакты (8-800-2000-122, 112), не продвигает шаг, не сохраняет как «факт»
- [x] Тесты: distress → true, идиомы/нейтральные → false (4/4); полный набор зелёный (14 файлов, 2965)
- [ ] ⚠️ ФИНАЛЬНЫЕ формулировки и контакты — на ревью подростковым психологом перед реальными детьми (черновик помечен в коде)

### A4. Telegram webhook fail-closed + токен не в URL
- [x] Webhook `503` при пустом `TELEGRAM_WEBHOOK_SECRET`; secret_token сверяется всегда (constant-time)
- [x] Все 4 ссылки входа переведены на одноразовый `exchange_token` (5 мин) через `buildTelegramLoginUrl`; raw session token убран из URL (0 вхождений)
- [x] Тест: 503 без секрета, 401 при неверном/отсутствующем токене (3/3); полный набор зелёный (15 файлов, 2968)

### A5. Privacy-страница по факту + лёгкое согласие родителя
- [x] `app/privacy/page.tsx` переписана под реальную схему (email, телефон, TG/MAX ID, IP/UA, переписка, ответы, отчёт, связь родитель-ребёнок) + права/удаление/согласие <18
- [x] Обязательная галочка «я родитель/законный представитель» в форме `app/lead/page.tsx` со ссылкой на /privacy
- [x] Submit заблокирован без согласия (`disabled` + guard + `parentalConsent`/`consentAt` в payload); tsc=0

### A6. Инцидент-гигиена
- [ ] Очищены старые небезопасные ключи `report:*`/`session:*`
- [ ] Логи проверены на утёкшие токены; при находках — ротация

---

## ФАЗА B — связность данных и логика подачи

### B1. Сквозная связность данных (единый источник истины)
- [x] Подтверждено: конвейер единого источника СУЩЕСТВУЕТ на финализации (`next-question`): coach + тесты → `computeConsistency` → `buildSummaryProfile` → `digitalProfile.upsert` + отчёт
- [x] Тест-инвариант связности (`layers.test.ts`): ответ КОУЧА и ответ ТЕСТА оба повышают покрытие единого профиля; профиль содержит входы всех 4 поверхностей (Колесо/Пирамида/матчинг/отчёт) — 3 новых теста зелёные
- [x] `coverage` в `buildSummaryProfile` = метрика связности (8 слоёв) — регрессионный guard от разрозненности
- [ ] ⏳ Инкрементальный `computeConsistency` при КАЖДОМ ответе (сейчас на финализации) — требует правки коуч-потока + прогон на приложении
- [ ] ⏳ Инвентаризация 40 полей `// TODO: нет источника` (назначить/удалить) — методологическая работа (Фаза E)

### B1a. Колесо и Пирамида в личном кабинете
- [ ] ⏳ Требует UI личного кабинета (этап 13) — переиспользовать `RadarChart`/`DeepSessionCard` из `DigitalProfile.summary`. Отложено: нужен запущенный кабинет для проверки (не покрывается тестами)

### B1b. Трек H — достройка паспортов (бо́льшая часть готова)
- [x] `salary` 204/204, `fact` 204/204, `subjects`, «почему подойдёт» + оси — УЖЕ ЕСТЬ
- [ ] `educationPath` (куда поступать) заполнен для всех профессий (сейчас 0/204)
- [ ] `outlook` (перспективы) заполнен с датировкой источника (сейчас 0/204)
- [ ] «Путь» и «Перспективы» видны в карточке

### B2. Честная подача результата (без ложных процентов)
- [x] `matchBand()` — «X%» заменён на качественную полосу (Сильное/Хорошее/Стоит присмотреться) в карточке и текстовом фолбэке (тест 4/4)
- [x] Ранжирование и архетипы сохранены (порядок не изменён, только подача)
- [x] Анти-детерминизм-дисклеймер под секцией профессий («направления, не приговор, решение за тобой»)
- [x] Разбивка «почему подходит» по осям сохранена; tsc=0, набор зелёный (16 файлов, 2972)

### B3. Границы «профориентация ≠ терапия»
- [ ] Дисклеймер «инструмент, не замена специалиста»
- [ ] Родителю по умолчанию — сводка, не сырой transcript

---

## ФАЗА C — дизайн/доступность/контент (к запуску пилота)
- [ ] C1. Ручная a11y с NVDA/VoiceOver по потокам; контраст ≥4.5:1; Target Size; Accessible Authentication
- [ ] C3. Проверка на dark patterns

---

## Легенда статуса
- [ ] не сделано · [x] сделано и проверено критерием приёмки.
- Прогресс отражать также в `docs/26` и полной дорожной карте (статус-слой этапов).

*По мере реализации отмечаем пункты и обновляем статус этапов в стратегических файлах.*
